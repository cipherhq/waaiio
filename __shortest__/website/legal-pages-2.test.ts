import { shortest } from "@antiwork/shortest";

shortest("Navigate to /terms, /cookies, /dpa, and /acceptable-use one by one and verify each loads with legal text content (or redirects to a valid legal page)");
