import { shortest } from "@antiwork/shortest";

shortest("On the login page, enter an invalid email 'notauser@test.com' with password 'wrongpassword' and submit. Verify an error message appears indicating invalid credentials");
